import dotenv from 'dotenv';
dotenv.config();

const config = {
    api_host : 'http://localhost:3000',
    site_title: 'Santri Penerima Amanah',
    owner: 'IT Support Munzalan',
    contact: 'adityapratamabadra@gmail.com'
}

export {config};